2002 m a l l e t m achine languag e ~mccallum 1 0 further ` l i e n s e author andrew mc callum <a href= mailto >mccallum edu< a> extract types tokenization sequence ?? document which a tokenization get document span get span i a span formed concatenating spans from start to end in more detail <ul> <li> start span will be start index <tt>get span start < tt> <li> end span will be start index <tt>get span end < tt> <li> unless <tt>start == end< tt> span will completely include <tt>get span start < tt> <li> span will never intersect <tt>get span end < tt> <li> <tt>start == end< tt> then span contains no text < ul> start index first token in span inclusive an index a token not an index into document end index first token in span exclusive an index a token not an index into document a span into tokenization s document span subspan start end 