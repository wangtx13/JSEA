2002 m a l l e t m achine languag e ~mccallum 1 0 further ` l i e n s e classify io serializable pipe types ada boost m2 <p> yoav freund and robert e schapire experiments a boosting algorithm in journal proceedings 13th international conference 1996 princeton ~schapire papers freund sc96b ps z author gary huang <a href= mailto ghuang >ghuang edu< a> ada boost m2 classifier serializable serial u = 1 l classifier weak classifiers alphas ada boost m2 pipe instance pipe classifier weak classifiers alphas instance pipe weak classifiers = weak classifiers alphas = alphas get number weak classifiers in ensemble classifier get num weak classifiers alphas length an ada boost m2 classifier that uses only first <tt>num weak classifiers to use< tt> weak learners <p> classifier s pipe and weak classifiers are backed respective classifier so changes to classifier s pipe and weak classifiers are reflected in classifier and vice versa ada boost m2 get trimmed classifier num weak classifiers to use num weak classifiers to use <= 0 || num weak classifiers to use > weak classifiers length illegal argument number weak learners to use out range + num weak classifiers to use classifier weak classifiers = classifier num weak classifiers to use arraycopy weak classifiers 0 weak classifiers 0 num weak classifiers to use alphas = num weak classifiers to use arraycopy alphas 0 alphas 0 num weak classifiers to use ada boost m2 instance pipe weak classifiers alphas classification classify instance inst classify inst weak classifiers length classify given instance using only first <tt>num weak classifiers to use< tt> classifiers trained during boosting classification classify instance inst num weak classifiers to use num weak classifiers to use <= 0 || num weak classifiers to use > weak classifiers length illegal argument number weak learners to use out range + num weak classifiers to use feature vector fv = feature vector inst get data instance pipe == || fv get alphabet == instance pipe get data alphabet num classes = get label alphabet size scores = num classes best index sum = 0 gather scores all weak classifiers round = 0 round < num weak classifiers to use round++ best index = weak classifiers round classify inst get labeling get best index scores best index += alphas round sum += scores best index normalize scores i = 0 i < scores length i++ scores i = sum classification inst label vector get label alphabet scores 