share upenn ner pipe types a feature approximating length length bins pipe io serializable name bins bin names <p>bins contains maximum sizes elements in each bin <p> example passing in 1 3 7 would produce 4 bins strings lengths 1 2 3 4 7 and 8+ length bins feature name bin maxes name = feature name bins = bin maxes arrays sort bins min = 1 bin names = bins length+1 i=0 i<bins length i++ bin names i = min == bins i ? +min+ +min+ +bins i + min = bins i +1 bin names bins length = +min+ + instance pipe instance carrier token sequence ts = token sequence carrier get data tokens i=0 i < ts size i++ token t = ts get i length = t get text length j=0 j<bins length j++ length <= bins j t set feature value name+ = +bin names j 1 0 tokens t set feature value name+ = +bin names bins length 1 0 carrier 